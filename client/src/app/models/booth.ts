export interface Booth{
  plaza_id: string;
  booth_id: string;
  name: string;
  email: string;
  password: string;
  total_collection: string;
  token: string;
  is_active: boolean;
  road_id: string;
  authority_id: string;
}
